#!/bin/bash

# このプロンプトを実行する際のコンテキスト制御の意図：
# 
# ■ 読ませたいファイル：
#   - なし
#   ※単純な挨拶であるため、TS規約や4層アーキテクチャのドキュメントは完全に無関係。
# 
# ■ 期待される挙動：
#   - 単一 AGENTS.md：無関係な TypeScript などの巨大なルール（約18KB）が無駄にロードされ、トークンを浪費する。
#   - 概要版 AGENTS.md：関係がないため docs/ 配下の読み込み（動的ロード）は発生せず、
#     必要最小限のコンテキストのままで回答が完了する。

PROMPT="hello"

agy -i "$PROMPT"
