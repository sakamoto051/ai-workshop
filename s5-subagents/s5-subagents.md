---
marp: true
theme: default
paginate: true
header: 'AI エージェント入門ワークショップ'
footer: 'S5: Sub-agents 編'
---

# S5. Sub-agents 編
## エージェントに「別の頭脳」を任せる

コンテキストの分離と並列委譲によるスケールアウト

---

## 本日のフロー (60 分)

| 時間 | パート |
|---|---|
| 0-5 | イントロ |
| 5-25 | 概念解説 (仕組み / コンテキスト分離 / 並列実行) |
| 25-35 | 講師デモ (調査系サブエージェントへの委譲) |
| 35-55 | ハンズオン (Subagent 作成 + 並列委譲) |
| 55-60 | 3 製品比較 + まとめ |

---

## このセッションのゴール

1. **Subagents が解決する課題を説明できる**
   コンテキスト汚染・専門性の欠如・逐次実行のボトルネックを理解する。
2. **独自の Subagent を定義できる**
   役割・利用ツール・呼び出しタイミングを設計できるようになる。
3. **並列委譲の使いどころを判断できる**
   「1つのエージェントに全部やらせる」から「役割分担」への転換を体感する。

---

## なぜ1つのエージェントで完結させないのか

単一のエージェントに全工程（調査・設計・実装・レビュー）を任せると:

- **コンテキストの逼迫**: 大量のファイル探索ログが会話に残り続け、上限に近づく
- **注意力の分散**: 「今は調査中なのか実装中なのか」でLLMの役割認識がぶれる
- **逐次実行しかできない**: 独立した調査を並行して進められず、時間がかかる

→　Skills が「**知識の遅延注入**」なら、Subagents は「**実行主体の分離**」

---

## Subagents とは

> 特定の役割・ツール権限・システムプロンプトを持つ、**独立したコンテキストで動く子エージェント**

- 親エージェント（オーケストレーター）が必要に応じて呼び出す
- 子エージェントは自分専用のコンテキストウィンドウで作業する
- 作業完了後、**要約された結果だけ**を親に返す（探索の途中経過は親に残らない）

---

## コンテキストの分離

<div style="display: flex; justify-content: space-between; align-items: flex-start; font-size: 25px;">
<div style="width: 48%;">

**親エージェントのコンテキスト**
- ユーザとの会話
- タスクの全体方針
- Subagentへの依頼と**要約結果**のみ

</div>
<div style="width: 48%;">

**Subagent のコンテキスト**
- 役割の指示（システムプロンプト）
- 具体的なタスク（委譲時の指示）
- 探索したファイルやツールのログ
- 親には見せない作業過程

</div>
</div>

→ 親の会話は汚れず、子の探索ログは子の中で使い捨てにできる

---

## コンテキスト分離のトレードオフ (デメリット)

メリットの一方で、以下のトレードオフ（制限）に注意する必要があります:

- **前提知識の不足**: 子は親のすべての会話履歴を参照できないため、意図の汲み取り漏れが起きやすい
- **試行錯誤プロセスの損失**: 親には要約結果しか返らないため、失敗したアプローチの経緯などが伝わらない
- **コスト・オーバーヘッド**: 子の起動ごとにシステムプロンプトが消費され、トークン消費や実行時間が増える

→ 役割を明確に定義し、適切な背景情報を渡して依頼することが不可欠

---

## Antigravity (agy) の Subagents 定義 (1/3)

<div style="font-size: 27px;">

ワークスペースの `.agents/agents/<エージェント名>/agent.md` に定義します。
シンプルなエージェントは配置するだけで自動認識されます（インストールコマンドや再起動は不要です）。

配置場所の例:
- `.agents/agents/researcher/agent.md`
- `.agents/agents/orchestrator/agent.md`

TUI上で **/agents** コマンドを実行することで、利用可能なエージェントの一覧を確認できます。

</div>

---

## Antigravity (agy) の Subagents 定義 (2/3)

`.agents/agents/researcher/agent.md`:
```markdown
---
name: researcher
description: コードベースの調査を頼まれたときに使う。
  ファイル探索やキーワード検索が主目的のときに呼び出すこと。
tools: Read, Grep, Glob
model: gemini-flash
---

あなたはコードベース調査の専門家である。
実装は行わず、該当箇所の特定と要約のみを行うこと。
```

---

## Antigravity (agy) の Subagents 定義 (3/3)

- **`description`**: 親エージェントが「いつ呼ぶか」を自動判定する起動条件
- **`tools`**: サブエージェントに許可するツール（誤操作や事故を防ぐ境界）
- **`model`**: 処理の重さに応じた推論モデルの指定（gemini-flash / gemini-pro など）

---

## 呼び出しのされ方

Antigravity (agy) では 2 通りの起動経路と、確認用UIが提供されています:

1. **自動委譲**: `description` に合致するタスクだと親が判断すると自動的に起動
2. **明示的な指示**: プロンプトで「`researcher` を起動して」と指名

- **`/agents` パネル**: CLIで `/agents` コマンドを実行し、起動中の子エージェント一覧を確認・監視可能
- **`ctrl+j` / `ctrl+k`**: 承認待ちのタスクへ移動（`ctrl+j`）したり、その場で承認（`ctrl+k`）するショートカット

---

## 並列委譲というもう1つの価値

Subagents は **同時に複数呼び出せる**。

```
親: 「フロントエンド・バックエンド・インフラの
      3領域を同時に調査して」
   ├→ frontend-researcher（並列実行）
   ├→ backend-researcher（並列実行）
   └→ infra-researcher（並列実行）
      └→ 3つの要約が出そろってから親が統合
```

→ 逐次実行では3倍かかっていた調査が、並列化で短縮できる

---

## 講師デモ: 調査系 Subagent への委譲 (10 分)

用意済みの `researcher` サブエージェントに対して:

> 「このリポジトリの認証処理がどのファイルに実装されているか調べて」

**注目ポイント**:
- メインの会話には最終的な要約だけが残り、探索の生ログは残らない
- `/agents` (または相当のコマンド) で呼び出された Subagent を確認できる

---

## ハンズオン #1: 独自 Subagent の作成 (1/2) (8 分)

`.agents/agents/reviewer/agent.md` を新規作成する:

```markdown
---
name: reviewer
description: コード変更のレビューを頼まれたときに使う。
tools: Read, Grep, Glob
---

あなたはシニアエンジニアとしてコードレビューを行う。
- セキュリティ上の懸念
- エラーハンドリングの抜け
- 命名や責務分割の妥当性
の3観点で指摘すること。実装の修正は行わない。
```

---

## ハンズオン #1: 独自 Subagent の作成 (2/2)

作成後、「直前の変更をレビューして」と依頼し、`reviewer` が呼ばれることを確認する。

- メインの会話に `reviewer` への委譲ログが残ることを確認する
- 指摘内容が3観点（セキュリティ / エラーハンドリング / 命名・責務分割）に沿っているか見る

<!-- 詰まったグループには hands-on/answer-key/reviewer.md を案内する -->

---

## ハンズオン #2: 並列委譲を体感する (12 分)

3つの Subagent を定義・用意します。

- `frontend_researcher/agent.md` (フロントエンド調査)
- `backend_researcher/agent.md` (バックエンド・DB調査)
- `orchestrator/agent.md` (オーケストレーター / tools: Subagents)

> 認証処理とDB設計について orchestrator を起動して調査を任せて

メイン ➔ orchestrator ➔ 子エージェントの自動連携を確認します。

---

## S5ハンズオンの実装構成 (3階層の並列委譲)

親が直接子を呼ぶのではなく、**親 ➔ orchestrator ➔ 子** という3階層構成です。

```
親 (プロンプトで orchestrator を起動)
  └─→ orchestrator (子 / tools: Subagents)
        ├─→ frontend_researcher (孫 / フロント調査)
        └─→ backend_researcher (孫 / バックエンド調査)
```

- `orchestrator` は YAML で `tools: Subagents` を持ち、2つの子を自律的に起動します。

---

## ハンズオン #3 (任意・時間が余れば): ツール制限による事故防止

`reviewer` の `tools` から `Edit` / `Write` を意図的に外してあることを確認する。

> 「reviewer にレビューだけでなく、直接コードも直させて」

と依頼し、**ツール権限がない Subagent は書き込めない**ことを確認する。

→ 「役割ごとに権限を絞る」ことが事故防止に直結する

---

## Skills との違い

| | Skills (S3) | Subagents (S5) |
|---|---|---|
| 何を分離するか | **知識**（手順・ルール） | **実行主体**（コンテキストそのもの） |
| 実行コンテキスト | 親と同じコンテキストに注入 | 独立した別コンテキスト |
| 並列実行 | 不可 | 可能 |
| 向いている用途 | ルールの遅延読込 | 重い調査・専門ペルソナ・並列作業 |

→ 併用も可能: `reviewer` Subagent が内部で `review` Skill のルールに従う、など

---

## 設計の指針

- **役割は1つに絞る**: 「何でも屋」の Subagent は description が曖昧になり呼ばれにくい
- **`tools` は必要最小限**: 調査系なら読み取り専用ツールだけを許可する
- **要約の粒度を意識する**: 親に返す結果が長すぎると分離した意味が薄れる
- **並列化できるタスクを見極める**: 依存関係のある工程は並列に向かない

---

## 3 製品の Subagents 対比

<div style="font-size: 21px;">

| 観点 | Antigravity (agy) | Codex CLI | Claude Code |
|---|---|---|---|
| 専用の Subagent 定義機構 | ◯ （`.agents/agents/`） | 未提供（外部プロセス連携が中心） | ◯ （`.claude/agents/*.md`） |
| ツール権限の個別制限 | ◯ | - | ◯ |
| 並列委譲 | ◯ （非同期に複数起動可能） | △（複数プロセス起動で代替） | ◯ |

→ **Antigravity (agy)** と **Claude Code** は、第一級の機能として Subagents 委譲をサポートしています。

</div>

---

## まとめ

- Subagents = **実行コンテキストそのものの分離**（Skills は知識の分離）
- メインの会話を汚さずに、重い調査や専門作業を任せられる
- **並列委譲**で、逐次実行のボトルネックを解消できる
- 役割を絞り、ツール権限を最小化することが事故防止にもつながる

次回 (S6): すべてを組み合わせた **総合演習 + ROI 測定**

---

## 参考

- Google Antigravity CLI Subagents ドキュメント
- Claude Code Subagents ドキュメント
- Codex CLI のマルチプロセス連携パターン
