# AI-Agent とは？ (主要エージェントと Copilot との違い)

## 1. AI エージェントの定義
AI エージェント（AI Agent）とは、単にユーザーの入力に対して一度だけ回答するLLMチャット（アシスタント）とは異なり、**「与えられた目標に向けて、自律的に思考（Plan）・道具を適用（Action）・結果を確認（Observe）するループ（ReActなど）を回し、自律的にタスクを完了させるシステム」** です。

| 項目 | 従来のAIチャット/アシスタント | AI エージェント |
|---|---|---|
| **動作モデル** | リアクティブ（一問一答） | プロアクティブ（自律ループ） |
| **ツール利用** | ユーザーがコピペや指示で適用 | エージェント自身が判断して実行（ファイル編集、コマンド実行） |
| **目標達成** | ユーザーが指示を出し続ける | 最初の目標設定だけで、自律的に試行錯誤する |

---

## 2. 主要な AI エージェントツール
現在、ソフトウェア開発領域で広く使われている主要な CLI 型エージェントは以下の通りです。

### 1) Claude Code (Anthropic)
- **特徴**: ローカルコードの広範な把握、テスト実行、Git操作、バグ修正を極めて自律的かつ高精度に実行。
- **拡張性**: Markdown形式の `CLAUDE　.md` や、専門特化した人格を並列で動かす `Sub-agents` 機能が特に強力。

### 2) Codex CLI (OpenAI)
- **特徴**: OpenAI のモデルをバックエンドにし、`config.toml` やカスタムプロンプトによるシンプルな拡張が可能。
- **功績**: プロジェクトの規約を明文化してエージェントに読み込ませる **`AGENTS.md`** の運用を業界標準に押し上げた元祖。

### 3) Antigravity CLI / 旧 Gemini CLI (Google)
- **特徴**: Goで書かれた高速なネイティブバイナリ。ローカルでの動作が非常に軽快。
- **拡張性**: **`MCP` (Model Context Protocol)** による外部ツール接続、Markdown形式の **`Skills`** によるカスタム命令、ツール実行をインターセプトして安全制御や自動整形を行う **`Hooks`** システムが標準化されている。

---

## 3. GitHub Copilot は AI エージェントか？
結論から言うと、**「従来のインライン補完やサイドバーチャットは AI エージェントではない（アシスタント）が、最新の Workspace や Agent ツールは AI エージェントである」** と整理できます。

### ❌ エージェントではない部分（補完・チャット）
*   インラインのコード補完や、サイドバーでの質問（GitHub Copilot Chat）は、ユーザーの入力（トリガー）に対して一度だけ応答する「一問一答」のアシスタントです。
*   エージェントのように、コードを書き換えた後に「テストを実行してエラーが出たからコードを再修正する」といった自律的な試行錯誤のループ（ReAct ループ）を持っていません。

### ⭕ エージェントである部分（Copilot Workspace / @workspace / Agent）
*   **GitHub Copilot Workspace**: イシューを元に仕様を定義し、タスクを分解し、コードを自律修正してビルド・テストを実行する、本格的な「AI エージェント」として動作します。
*   **Copilot Agent / チャットの拡張**: `@workspace` でファイルを横断的に検索し、自律的にコードを生成・編集して検証するプロセスはエージェントそのものです。

---

## 4. AI エージェントなのか？ CLI ツールなのか？
結論から言えば、これらは**「AI エージェントとしての自律的な実行能力を持った、開発者向けの CLI ツール（＝ CLI型 AI エージェント）」**と呼ぶのが最もふさわしいです。

*   **インターフェースとしての形態：CLI ツール**
    *   ターミナル上で動作し、コマンド（`agy` や `claude`）で起動する操作体系は、従来のデベロッパーツール（`git`、`npm`、`docker` 等）と同じです。
*   **システムの本質的な振る舞い：AI エージェント**
    *   従来の CLI ツールは「与えられた命令を一度だけ実行して終了」するのに対し、これらは目標（例: "バグを直してテストを通して"）を与えると、**「自律的にファイルを検索 $\rightarrow$ 原因特定 $\rightarrow$ コード書き換え $\rightarrow$ テスト実行 $\rightarrow$ エラー検証」という試行錯誤の ReAct ループ**を自ら回し続けます。

したがって、操作形態は「CLI ツール」ですが、その中身と実態は「AI エージェント」と呼ぶのが適切です。

---

## 5. 主要モデルのコンテキストウィンドウ比較（事実情報）
各CLIエージェントの裏側で動く主要な最新LLM（2026年現在）のコンテキストウィンドウ（入力+出力トークンの最大値）の比較です。

*   **Google (Gemini)**
    *   **Gemini 3.5 Flash**: 1,048,576 (1M) トークン
    *   **Gemini 3.1 Pro**: 1,000,000 (1M) トークン
*   **Anthropic (Claude)**
    *   **Claude Opus 4.8**: 1,000,000 (1M) トークン
    *   **Claude Sonnet 4.6**: 1,000,000 (1M) トークン
    *   **Claude Haiku 4.5**: 200,000 (200K) トークン
*   **OpenAI (GPT)**
    *   **GPT-5.5**: 1,050,000 (1M) トークン
    *   **GPT-5.4**: 1,050,000 (約1M) トークン
    *   **GPT-5.4 Mini**: 400,000 (400K) トークン

---

## 🔗 関連ノート (Vault内)
- [[agent-knowledge-vault/01_Concepts/AI Agents|AI Agents (エージェントの基本概念)]]
- [[agent-knowledge-vault/02_Tools/Antigravity CLI|Antigravity CLI (agy の仕様)]]
- [[agent-knowledge-vault/02_Tools/Claude Code|Claude Code の仕様]]
- [[agent-knowledge-vault/02_Tools/Codex CLI|Codex CLI の仕様]]
- [[agent-knowledge-vault/03_Guides/Prompt Engineering|AGENTS.md とプロンプト設計]]
